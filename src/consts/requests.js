import {API_URl} from '@env';

const requests = {
    BASE_URL: process.env.BASE_URl,
    LOGIN: '/users/login',
    SIGNUP: '/users',
    USER: '/users',
    TRAINING: '/trainings',
}

export default requests;
